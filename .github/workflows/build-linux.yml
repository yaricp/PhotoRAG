name: Build Linux

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-frontend:
    name: Frontend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: node --require ./scripts/crypto-polyfill.cjs ./node_modules/.bin/vitest run

  test-backend:
    name: Backend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install -r requirements-ci.txt
      - run: python -m pytest tests/ -v

  build-appimage:
    name: Build Linux AppImage
    runs-on: ubuntu-latest
    needs: [test-frontend, test-backend]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install system dependencies for electron-builder
        run: sudo apt-get update && sudo apt-get install -y libfuse2

      - name: Download Linux Python runtime
        run: bash scripts/download-python-linux.sh

      - name: Install npm dependencies
        working-directory: frontend
        run: npm ci

      - name: Build AppImage
        working-directory: frontend
        run: npm run dist:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify AppImage was produced
        run: |
          APP=$(find frontend/dist-electron -name "*.AppImage" | head -1)
          if [ -z "$APP" ]; then
            echo "ERROR: No .AppImage found in frontend/dist-electron/"
            ls -la frontend/dist-electron/ || true
            exit 1
          fi
          SIZE=$(du -sh "$APP" | cut -f1)
          echo "AppImage: $APP ($SIZE)"

      - name: Run bundle structure assertions
        run: bash scripts/test-bundle-linux.sh

      - uses: actions/upload-artifact@v4
        with:
          name: PhotoRAG-Linux-x86_64
          path: frontend/dist-electron/*.AppImage
          retention-days: 14
